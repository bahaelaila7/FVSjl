# UT #147 oak — mortality FIXED, but a NEW dense-oak BA under-growth (likely the #150 crown-init gap)

## Measurement (stand 1589563867290487, 11269 TPA sp814 oak, ut_sub.db vs FVSut_clean)
  year   liveTPA:BA   jlTPA:BA
  2021   10969:35     10969:35    (cyc0 bit-exact)
  2031   10663:109    10663:67    (TPA BIT-EXACT; BA jl 67 vs live 109 = ~61%, ~40% UNDER)
⇒ #147's oak MORTALITY over-kill is FIXED (TPA bit-exact both cycles). The residual is now a GROWTH under-prediction:
jl grows the dense oak's BASAL AREA ~40% too little (35→67 vs live 35→109 in one cycle).

## Root hypothesis — the #150 crown-init gap, SEVERE for UT woodland/oak (I mis-classified UT "cornered")
- UT setup (simulate.jl:92-97) does NOT call crown_ratio_update!(lstart) — like most variants, only CR/CI/BM(now) do.
  ⇒ dense READ oak seedlings keep crown_pct=0.
- UT crown.jl:41: hardwoods/PJ (11-19,22,24 incl. oak) are SKIPPED by the Weibull crown update — "get crown from
  REGENT". So their crown stays 0 until REGENT small-tree runs.
- UT diameter_growth.jl:112 reads `cr = crown_pct·0.01` in the DG DDS. crown=0 ⇒ the crown DG term = 0 ⇒ under-grow.
⇒ SAME mechanism as BM #149 (crown=0 → small-tree growth starved), but it shows SEVERELY for OAK/woodland (crown-
sensitive DG) where I'd earlier called UT "cornered" — that verdict was from CONIFER stands (2860048/2916642/2381662,
BA within ±1-7), which have a healthier crown=0 behavior. The WOODLAND/hardwood species expose the crown-init gap.
⇒ This REFINES #150: the crown-init gap is severe not only for BM (VIGOR conifers) but ALSO for UT woodland/hardwood
(crown-dependent DG). The fix is the same: port the UT lstart crown-init (ut/dubscr.f or the REGENT crown dub) so oak
seedlings get a crown before the DG reads it.
NEXT (verify): instrument jl's oak seedling crown_pct at cyc1 (expect 0) + the DG crown term; confirm live assigns a
crown (dubscr) that jl skips. Then port UT lstart crown-init (mirror BM #149). #147 mortality = DONE; this is a
distinct GROWTH residual = the #150 crown-init gap for UT woodland.

## CONFIRMED (jl instrument) — UT oak seedlings have crown_pct=0 → the crown-init gap
Dumped jl UT DG crown for d<1 on stand 1589563867290487: oak seedlings sp=13(OH), d=0.086/0.1 → crown_pct=0; a
larger oak d=0.947 → crown_pct=70 (Weibull/REGENT-dubbed later). ⇒ CONFIRMED: the dense sub-1" oak seedlings enter
the UT DG with crown=0 (no lstart crown-init), so the DG crown term (cr=crown_pct·0.01) is 0 ⇒ suppressed DG ⇒ the
~40% BA under-growth (67 vs 109). This is the #150 crown-init gap, SEVERE for UT woodland/oak (crown-dependent DG),
which my earlier "UT cornered" verdict missed (it was based on conifer stands). FIX = port the UT lstart crown-init
(ut/dubscr.f, mirror BM #149 c29c1c7 / CI): dub the read sub-1" crown before the DG reads it. Then oak seedlings get
a crown → DG grows → BA closes toward 109. ⇒ #147 mortality DONE (TPA bit-exact); this GROWTH residual is a real
#150-class fix for UT.
