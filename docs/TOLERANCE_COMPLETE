TOLERANCE CAMPAIGN COMPLETE — 2026-07-05

Every numerical tolerance in the FVSjl test suite is in exactly one of three states:
  (1) BIT-EXACT (== / rendered-== against the live-Fortran golden), tolerance 0;
  (2) PROVEN-IRREDUCIBLE category-2, cornered to a named low-level Float32 op at its exact
      measured floor (grown-cycle ^power/exp transcendental; jl-exp vs libm-EXPF; non-associative
      sum order; print half-width against a rounded oracle field; DBS/econ/dvee stamps);
  (3) @test_broken — 4 documented divergences, each with a both-sides traced verdict:
        • COMPRESS s22   — IBM eigensolver Jacobi ULP (eigen.f), faithful port, sub-ULP partition tie.
        • WK3/DGSCOR     — sp33/65 past-dbh calibration tail.
        • timeint TPA    — non-native 10-yr cycle mortality-timing (SN calibrates at YR=5).
        • timeint cuft   — non-native 10-yr cycle WK3-COR/DGSCOR divergence (deferred YR-vs-FINT
                           calib split; bit-exact 4 cycles then compounds — a divergence, not a floor).

Full suite: 7645 pass / 4 broken / 0 fail.

The two blockers the prior FINAL VERDICT left open were both resolved this session by re-tracing
their labels against FVS source (not test behavior):
  (a) estab_pccf  — the "multi-point PCCF approximation" was a real point-assignment bug
                    (plot_id=nn vs IPTIDS[nn]); FIXED, distribution now == live; residual 0.10 is
                    the Float32 per-point-PCCF/INT-round boundary floor (7/10 points bit-exact).
  (b) LS flame/scorch — the "deferred crown-ratio-timing" was refuted: fire phase is source-verified
                    aligned (FMBURN before CROWN in both jl and gradd.f); the residual is jl Float32
                    exp(bcr4·D) vs libm EXPF() in crnew flipping ICR on the trunc(+0.5) boundary =
                    the goal's own transcendental class. Bound already at exact floor.

Full ledger: docs/TOLERANCE_AUDIT.md (Sessions 2026-07-05 … 2026-07-05aa).
