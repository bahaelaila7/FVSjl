#!/usr/bin/env bash
# Active campaign: WESTERN FVS variant cluster + extensions rollout (NOT a single variant).
# Goal: docs/WESTERN_ROLLOUT_GOAL.md ; off-switch: touch docs/WESTERN_ROLLOUT_COMPLETE.
DONE=/workspace/FVSjl/docs/WESTERN_ROLLOUT_COMPLETE
GOAL=/workspace/FVSjl/docs/WESTERN_ROLLOUT_GOAL.md
[ -f "$DONE" ] && exit 0
echo "=== ACTIVE GOAL (Stop hook) — Western FVS variant cluster + extensions rollout ===" >&2
cat "$GOAL" >&2
echo "=== DISCIPLINE: validate bit-exact vs LIVE FVS per chunk; MEASURE don't infer; per-record treelist diff INVALID after tripling (use .sum / pre-split); reuse the shared engine. Do NOT stop at one variant. DONE-or-cornered: #140(BM), #137(EM self-thin), EM-sub-inch-DG(crown-dub), #191(TT aspen), #143(IE pvref1 + EM AUTOES re-measured cornered +4-10% TPA, BA/TopHt match), #194(CI esgent birth-cycle fixed, transition cornered), CI-vol(bit-exact @cyc0), Climate-FVS. ★★ 2026-08-12 LATEST (6 commits): UT/TT CROWN-DUB FAMILY fixed — d<1 sub-1\" inventory-seedling DUBSCR (TT#198 862f320 / UT#199 f3edc5b) + d≥1 WOODLAND-CL crown (UT#203 a0e8cee / TT#204 2249737; CI clean) + the BROAD UT double-CRATET-site-conversion bug (12dbc6e, PP DDS now bit-exact, ~5-8% under-growth on all UT RM-site stands). ⚠ CORRECTION: \"WHOLE CLUSTER bit-exact-or-cornered\" holds at CYC0 + LARGE-TREE DG; MULTI-CYCLE real-FIA has SMALL-TREE/WOODLAND/ASPEN residuals the ref stands MASK. #206 utt01 −9%@2090 RESOLVED-CORNERED 2026-08-12 (FVSut_g16: large-tree DDS + DGSCOR calib BIT-EXACT all 27 trees; root = DGSCOR/OLDRN serial-corr straddle, dgdriv.f:795 gated DGSD≥1 fires under NOTRIPLE). ★★ DOCTRINE FIX: \"NOTRIPLE=deterministic\" is IMPRECISE — OLDRN serial-corr residual still fires (DGSD≥1, all western); prove real-vs-cornered by g16 per-tree DDS, not by NOTRIPLE. #205 TT aspen +34% over-growth FIXED 2026-08-13 (5b82bf8): FVStt_g16 root-caused TWO partially-canceling aspen-smhtgf bugs — (1) subcycle DOUBLE-APPLY (regent.f:415 gates aspen/CIVAR/UTVAR to J=1 only; jl subcycled every j → 2× for fint=10) + (2) SITAGE UNITS (findag.f:96 H·2.54·12 feet→cm metric; jl used feet). Old 'don't add ·2.54·12' comment was a two-bug artifact. BA +34%→±7% cornered; measured jl htgr=live g16. ★ LESSON: two canceling bugs hide behind a high-site 'validated' ref stand; a dense/low-site reproducer exposes them; fix BOTH (fixing one regresses). NO open pure-growth real-FIA residual remains cluster-wide. #196: BC metric-DATABASE units bug FOUND+FIXED (42eb555, cyc0 bit-exact TPA/SDI/TopHt/QMD); REMAINING = YSM multi-cycle under-mortalization ISOLATED to MISSING dwarf mistletoe: DM-free V3 control mrun/all_BC.key shows jl's BC V3 self-thin MATCHES oracle (TPA 2089→1253 vs 2087→1292), so YSM extra mort = the unported NEWSPRED spatial DM (canada/newmist ~50 rtns incl DMAUTO) + BC not in DM dispatch. FIX = port canada/newmist NEWSPRED (~8761 lines/~55 rtns SPATIAL DM, variant-comparable) + wire BC — a MAJOR multi-session port of an OPTIONAL model, niche value (only exercised case = crash-blocked YSM DATABASE), validation A/B crash-blocked ⇒ COST/BENEFIT is a USER decision. base mistoe.f done N-Rockies+CR. + BC V2/non-ICH. Off (USER's call): touch docs/WESTERN_ROLLOUT_COMPLETE ===" >&2
exit 2
